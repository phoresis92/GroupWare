<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
	 PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	 "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="project.groupware.apv_Vacat.VacatDAO">

	<select id="selectMember_vacation" resultType="double">
		select member_vacation from member where member_id = #{member_id}
	</select>

</mapper>